services:
  scoutline:
    build: .
    ports:
      - "${PORT:-8081}:8081"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - CACHE_FILE=/app/data/scoutline_cache.json
    restart: unless-stopped
